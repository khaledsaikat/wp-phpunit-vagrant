# Install PHPUnit and svn
sudo apt-get install -y phpunit
sudo apt-get install -y subversion

# Installing testing instance
# DB_NAME, DB_USER, DB_PASSWORD, DB_HOST, WP_VERSION
# sudo -u vagrant -i -- bash /home/vagrant/scripts/install-wp-tests.sh wp_tests homestead secret
